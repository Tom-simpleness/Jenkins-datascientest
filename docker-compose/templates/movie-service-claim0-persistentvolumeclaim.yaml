apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.movieService.persistentVolumeClaim.name }}
  labels:
    app: {{ include "docker-compose.name" . }}-movie-service
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.movieService.persistentVolumeClaim.storage }}
